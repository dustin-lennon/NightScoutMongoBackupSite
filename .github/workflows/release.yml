name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          # Use a PAT with admin rights to bypass branch protection rules
          # Check for common PAT secret names, fallback to GITHUB_TOKEN
          token: ${{ secrets.GH_PAT || secrets.GITHUB_PAT || secrets.RELEASE_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run tests
        run: bun run test

      - name: Run semantic-release
        env:
          # Use PAT with admin rights to bypass branch protection
          # Check for common PAT secret names: GH_PAT, GITHUB_PAT, RELEASE_TOKEN
          # The PAT needs 'repo' scope to bypass branch protection rules
          GITHUB_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_PAT || secrets.RELEASE_TOKEN || secrets.GITHUB_TOKEN }}
        run: bun run semantic-release
