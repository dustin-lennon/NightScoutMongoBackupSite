name: Deploy to Linode

permissions:
  contents: read

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Deployment environment"
        required: true
        default: "production"
        type: choice
        options:
          - production
          - staging
  push:
    branches:
      - main
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".github/**"
      - "!.github/workflows/deploy-linode.yml"

jobs:
  deploy:
    name: Deploy to Linode Server
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'production' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Dotenvx
        run: curl -sfS https://dotenvx.sh/install.sh | sh

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.LINODE_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.LINODE_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to Linode
        env:
          LINODE_HOST: ${{ secrets.LINODE_HOST }}
          LINODE_USER: ${{ secrets.LINODE_USER }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH || '/opt/nightscout-backup-site' }}

          # NextAuth and Discord secrets - passed directly from GitHub secrets
          NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
          DISCORD_CLIENT_ID: ${{ secrets.DISCORD_CLIENT_ID }}
          DISCORD_CLIENT_SECRET: ${{ secrets.DISCORD_CLIENT_SECRET }}
          ALLOWED_DISCORD_USER_ID: ${{ secrets.ALLOWED_DISCORD_USER_ID }}

          # DOTENV_PRIVATE_KEY_CI is optional - if not set in secrets, it should be configured on the server
          DOTENV_PRIVATE_KEY_CI: ${{ secrets.DOTENV_PRIVATE_KEY_CI || '' }}
        run: |
          dotenvx run -- ssh ${LINODE_USER}@${LINODE_HOST} "DEPLOY_PATH='${DEPLOY_PATH}' NEXTAUTH_SECRET='${NEXTAUTH_SECRET}' DISCORD_CLIENT_ID='${DISCORD_CLIENT_ID}' DISCORD_CLIENT_SECRET='${DISCORD_CLIENT_SECRET}' ALLOWED_DISCORD_USER_ID='${ALLOWED_DISCORD_USER_ID}' DOTENV_PRIVATE_KEY_CI='${DOTENV_PRIVATE_KEY_CI}' bash -s" << 'EOF'
          set -e

          echo "📦 Starting deployment to ${DEPLOY_PATH}..."

          # Navigate to deployment directory
          cd "${DEPLOY_PATH}"

          # Pull latest changes
          echo "🔄 Pulling latest changes..."
          git fetch origin
          git checkout main
          git pull origin main

          # 🧠 Make sure bun is available in PATH
          if [ -d "$HOME/.bun" ]; then
            export BUN_INSTALL="$HOME/.bun"
            export PATH="$BUN_INSTALL/bin:$PATH"
          fi

          # Also check common installation paths
          if ! command -v bun &> /dev/null; then
            if [ -f "/usr/local/bin/bun" ]; then
              export PATH="/usr/local/bin:$PATH"
            elif [ -f "/usr/bin/bun" ]; then
              export PATH="/usr/bin:$PATH"
            fi
          fi

          # 🧠 Make sure Node & PM2 are available
          export NVM_DIR="$HOME/.nvm"
          [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

          # Verify bun is available
          if ! command -v bun &> /dev/null; then
            echo "❌ Error: bun is not found in PATH"
            echo "Current PATH: $PATH"
            echo "Checking common locations..."
            ls -la ~/.bun/bin/bun 2>/dev/null || echo "~/.bun/bin/bun not found"
            ls -la /usr/local/bin/bun 2>/dev/null || echo "/usr/local/bin/bun not found"
            ls -la /usr/bin/bun 2>/dev/null || echo "/usr/bin/bun not found"
            exit 1
          fi
          echo "✅ Found bun at: $(which bun)"
          bun --version

          # Install dependencies
          echo "📚 Installing dependencies..."
          bun install --frozen-lockfile

          # Build Next.js application
          echo "🏗️  Building Next.js application..."
          bun run build

          # Export environment variables for PM2
          # These will be available to the Next.js application
          export NEXTAUTH_SECRET="${NEXTAUTH_SECRET}"
          export DISCORD_CLIENT_ID="${DISCORD_CLIENT_ID}"
          export DISCORD_CLIENT_SECRET="${DISCORD_CLIENT_SECRET}"
          export ALLOWED_DISCORD_USER_ID="${ALLOWED_DISCORD_USER_ID}"

          # DOTENV_PRIVATE_KEY_CI is optional - for dotenv-vault support
          # If provided, it will be used to decrypt .env.vault files
          if [ -n "${DOTENV_PRIVATE_KEY_CI:-}" ]; then
            export DOTENV_PRIVATE_KEY_CI="${DOTENV_PRIVATE_KEY_CI}"
            echo "✅ DOTENV_PRIVATE_KEY_CI provided (from GitHub secret or server environment)"
          else
            echo "ℹ️  DOTENV_PRIVATE_KEY_CI not provided - using direct GitHub secrets instead"
          fi

          echo "✅ Environment variables exported for PM2"

          # Restart the service with PM2
          # PM2 will inherit all exported environment variables when it starts
          echo "🔄 Restarting application..."
          bun run restart:pm2 || bun run start:pm2

          # Wait for health check
          echo "⏳ Waiting for application to start..."
          sleep 10

          # Check status
          echo "✅ Checking application status..."
          pm2 status

          # Check site status
          pm2 describe nightscout-backup-site || echo "⚠️  Site process not found"
          pm2 logs nightscout-backup-site --lines 20 --nostream || true

          # Test Next.js site health endpoint
          echo "🏥 Testing Next.js site endpoint..."
          if curl -f -s http://localhost:3000 > /dev/null; then
            echo "✅ Next.js site health check passed"
          else
            echo "⚠️  Next.js site health check failed (may still be starting)"
          fi

          echo "✅ Deployment complete!"
          EOF

      - name: Verify Deployment
        env:
          LINODE_HOST: ${{ secrets.LINODE_HOST }}
          LINODE_USER: ${{ secrets.LINODE_USER }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH || '/opt/nightscout-backup-site' }}
        run: |
          ssh ${LINODE_USER}@${LINODE_HOST} "DEPLOY_PATH='${DEPLOY_PATH}' bash -s" << 'EOF'
            # 🧠 Make sure Node & PM2 are available
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

            cd "${DEPLOY_PATH}"

            # Check if site is online
            if ! pm2 describe nightscout-backup-site &>/dev/null; then
              echo "❌ Site process not found"
              exit 1
            fi
            
            # Check process status (using pm2 jlist for reliable JSON parsing)
            if command -v jq &> /dev/null; then
              PM2_STATUS=$(pm2 jlist | jq -r '.[] | select(.name=="nightscout-backup-site") | .pm2_env.status' || echo "unknown")
            else
              # Fallback: check if process appears in pm2 list
              PM2_STATUS=$(pm2 list | grep -q "nightscout-backup-site.*online" && echo "online" || echo "unknown")
            fi
            
            if [ "$PM2_STATUS" != "online" ]; then
              echo "❌ Site process is not online (status: $PM2_STATUS)"
              pm2 describe nightscout-backup-site
              exit 1
            fi
            echo "✅ Site process is running (status: $PM2_STATUS)"
            
            # Test Next.js site endpoint
            if curl -f -s http://localhost:3000 > /dev/null; then
              echo "✅ Next.js site is responding"
            else
              echo "❌ Next.js site health check failed"
              exit 1
            fi

            echo "✅ Application is running"
            EOF

      - name: Notify on Success
        if: success()
        run: |
          echo "✅ Deployment to ${{ github.event.inputs.environment || 'production' }} successful!"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"

      - name: Notify on Failure
        if: failure()
        run: |
          echo "❌ Deployment to ${{ github.event.inputs.environment || 'production' }} failed!"
          echo "Please check the logs above for details."
